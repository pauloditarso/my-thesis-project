  (EU ACHO QUE ESTAH RESOLVIDO!!!) (TESTAR, TESTAR, TESTAR!!!)
* problema encontrado:
	- 3 tasks finalizam na mesmo momento e, naquele tempo, existe uma tasks queued para ser executada. todas vao achar que existe
	  uma task nova para ser executada, apenas uma serah, e as outras nao vao ser doadas para a grade;
	- possivel solucao: adicionar o evento de schedule logo apos o termino das task;
	- para isso eh necessario inserir em um lugar especifico na fila de eventos, ou seja, alterar o insertevent.c;
	- tambem servirah para as entradas e incio de execucao de um job, antes da entrada de execucao de sua primeira task
	  (apenas para melhorar a visibilidade do trace de saida); 
	  
* implementar as funcoes de utility;
	como???
	- primeiro, fazer o registro (na accountList) de todas as execuções de tasks, inclusive as que falharam;
	(talvez não seja importante no primeiro momento, pois assume-se que apenas as máquinas na grade falham)
	(mas as máquinas na cloud podem falhar também, caso contrato acabe, por exemplo)
	- fazer um registro similar ao accountList para os Jobs;
	(a partir deste registro e da função de utility a gente contabiliza o utilidade produzida)

* implementar o balancing do grid;
	como calcular o balancing?
	
* implementar a fase de planning (com o estabelecimento dos contratos);
	(isso implica na entrada das maquinas da cloud)
	(nao faco ideia como serah o processo de otimizacao) :(
	(lembrar da solucao de soh comprar maquinas na cloud para a hora de precisa, ou seja, a ultima)
	(fazer um pre-processamento da quantidade necessária para contratação na cloud)